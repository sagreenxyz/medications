---
import BaseLayout from './BaseLayout.astro';

export interface Props {
  title: string;
  drug?: any;
}
const { title, drug } = Astro.props;
---
<BaseLayout title={title}>
  <slot />
</BaseLayout>
